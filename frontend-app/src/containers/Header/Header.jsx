import Navbar from '../../components/Navbar/Navbar'; 

const Header = ({currentUsername}) => {
    return (
        <header>
            <Navbar />
            <h1>Journey</h1>
        </header>
    )
}

export default Header;